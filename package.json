{
    "name": "@aurogpt/flowgpt",
    "license": "MIT",
    "version": "1.0.0",
    "type": "module",
    "main": "dist/index.js",
    "module": "dist/index.mjs",
    "types": "dist/index.d.ts",
    "scripts": {
        "release": "yarn build && changeset publish",
        "build": "tsup src/*.ts src/**/*.ts --dts --format esm,cjs --minify --clean --onSuccess 'cp -r src/contents dist/'",
        "lint": "tsc src/*.ts"
    },
    "devDependencies": {
        "@changesets/cli": "^2.27.11",
        "@types/bun": "latest",
        "tsup": "^8.3.5"
    },
    "peerDependencies": {
        "typescript": "^5.7.2"
    },
    "dependencies": {
        "@langchain/core": "^0.3.27",
        "@langchain/openai": "^0.3.16",
        "@types/node": "^22.10.3",
        "langchain": "^0.3.9"
    }
}
